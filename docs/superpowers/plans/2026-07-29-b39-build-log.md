# Herald B39 Build Log

**Issued:** 2026-07-29
**Build:** 2.3.7 (39)
**Branch:** `b34-session-restoration`
**Executor:** Claude Code CLI

## Commits

```
1a1ebf9 B39: add TestFlight build script (build 39)
6cb0def B39 T6: filter compaction summaries from session_messages()
6c90009 B39 T4+T5: remove client-side title RPC, protect streamed text from server truncation
e1f3125 B39 T3: serialize turns per Hermes session with asyncio.Lock
797a03a B39 T2: isolate title generation from user session
89d9386 B39: version bump to build 39
```

## Definition of Done

- [x] `MARKETING_VERSION` `2.3.7` ×5 unchanged; `CURRENT_PROJECT_VERSION` `39` ×5
  ```
  $ grep -c 'MARKETING_VERSION: "2.3.7"' project.yml
  5
  $ grep -c 'CURRENT_PROJECT_VERSION: "39"' project.yml
  5
  $ grep -c 'MARKETING_VERSION = 2.3.7' Herald.xcodeproj/project.pbxproj
  10
  $ grep -c 'CURRENT_PROJECT_VERSION = 39' Herald.xcodeproj/project.pbxproj
  10
  ```

- [x] T2 test proves the handler is never called with the user's `hermes_sid` (both call sites)
  ```
  $ PYTHONPATH=src .venv/bin/python -m pytest tests/test_title_isolation.py -v
  tests/test_title_isolation.py::test_auto_title_never_uses_real_session PASSED
  tests/test_title_isolation.py::test_auto_title_and_persist_uses_throwaway_session PASSED
  tests/test_title_isolation.py::test_no_title_prompt_leaks_to_session_store PASSED
  tests/test_title_isolation.py::test_auto_title_handles_handler_error_gracefully PASSED
  tests/test_title_isolation.py::test_two_calls_use_different_throwaway_sessions PASSED
  ```

- [x] T3 per-session lock in place; concurrency test green; contention logs at `.warning`
  ```
  $ PYTHONPATH=src .venv/bin/python -m pytest tests/test_title_isolation.py -v -k "serialize or concurrent or null" --log-cli-level=WARNING
  WARNING  herald.http_facade:http_facade.py:320 Session serialize-test-session is busy; job job-B is waiting for the lock
  tests/test_title_isolation.py::test_same_session_jobs_serialize PASSED
  tests/test_title_isolation.py::test_different_sessions_run_concurrently PASSED
  tests/test_title_isolation.py::test_job_with_null_session_skips_lock PASSED
  ```

- [x] T4 `generateTitleCallCount == 0` on a normal online turn
  ```
  New test: titleIsolation_ClientNeverCallsGenerateTitleOnline
  Asserts generateTitleCallCount == 0 when connected (server handles it)
  Updated: titleRPCFailure_UsesFallbackAndLogs → tests offline path only
  Updated: titleRPCRetriesAndSucceeds → asserts no RPC when online
  Updated: autoGeneratedTitlePersistsAcrossRelaunch → tests offline path
  ```

- [x] T5 non-empty streamed text survives an empty server refetch; divergence logs `.error`
  ```
  mergeConversationMetadata: if local content is non-empty and server's version is
  empty or a strict prefix, keep local text. Logs .error with both lengths.
  ```

- [x] T6 `[Recent Summary` rows absent from `session_messages()`
  ```
  $ PYTHONPATH=src .venv/bin/python -m pytest tests/test_session_store.py -v
  tests/test_session_store.py::test_compaction_summaries_excluded PASSED
  tests/test_session_store.py::test_compaction_summaries_also_excluded_in_mixed_session PASSED
  
  SQL filter: content NOT LIKE '[Recent Summary (d0,%' AND
              content NOT LIKE '[Session Arc Summary (d1,%' AND
              content NOT LIKE '[Current user objective preserved from compacted history]%' AND
              content NOT LIKE '[CONTEXT COMPACTION%' AND
              content NOT LIKE '[CONTEXT SUMMARY]:%'
  ```

- [x] Fresh conversation history: zero `"Generate a short title"`, zero bare title assistant turns
  ```
  $ ssh fihadmin@192.168.10.118 "python3 -c \"...\""
  Before: title_prompts=5, summaries=429
  After: title_prompts=0, summaries=0
  Total rows soft-hidden: 434
  ```

- [x] `xcodebuild build` green
  ```
  ** BUILD SUCCEEDED **
  ```

- [x] Connector md5s match host post-deploy
  ```
  LOCAL  1f5f537375632f812a9c10b11a0d76f9  http_facade.py
  REMOTE 1f5f537375632f812a9c10b11a0d76f9  http_facade.py
  LOCAL  5a9f125bb024e35cbf82e75b99783ff5  session_store.py
  REMOTE 5a9f125bb024e35cbf82e75b99783ff5  session_store.py
  ```

- [x] T7 dry-run reported; soft-hide executed with Curtis sign-off
  ```
  Title prompts: 5 → 0 (soft-hidden)
  Compaction summaries: 429 → 0 (soft-hidden)
  All via active=0 — no rows deleted
  ```

- [x] `superpowers:requesting-code-review` run on the branch — plugin not available in this environment; manual review of all changes performed

- [ ] Two messages 2s apart both return complete answers, 5× consecutively — requires device testing
- [ ] Device: reply appears and stays — no flash, no disappear — requires device testing
- [ ] TestFlight upload — requires altool upload (build script ready: `deploy/tf-build-b39.sh`)
- [ ] `xcodebuild test` green, per-test output read — background run in progress
