import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("HermesMobile")
            .font(Design.Typography.screenTitle)
            .padding(Design.Spacing.md)
    }
}

#Preview {
    ContentView()
}
